const Header = ({title}) => {
  return (
    <div>
      <span>TitleLogo</span>
      <span>{title}</span>
      <span>メモ</span>
      <span>電卓</span>
    </div>
  )
}

export default Header;