const Header = ({title}) => {
  return (
    <div>
      <span><a href='/'>TitleLogo</a></span>
      <span>{title}</span>
      <span>メモ</span>
      <span>電卓</span>
    </div>
  )
}

export default Header;